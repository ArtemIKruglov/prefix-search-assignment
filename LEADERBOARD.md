# Prefix Search Assignment Leaderboard

| Место | Кандидат | Ручная оценка | Комментарий |
| --- | --- | --- | --- |
| 1 | OpenSearch Prototype (Codex) | 25/25 (100 %) релевантных запросов | OpenSearch 3.3.2 + Redis; mixed analyzers (RU↔EN, layout fix), measurement fallback и популярностные бусты. Ручной прогон 25‑префиксной выборки (`reports/manual_sample_opensearch_20251110_patterns_v4.csv`) прошёл без нулей. |
| 2 | DyxDeda | 17/20 (85 %) релевантных запросов | FastAPI + ES, агрессивная нормализация + мини-словарь запросов. Ломается на «холс», «кар тофель», «хозяйст мыло» (см. `reports/manual_sample_DyxDeda_prefix_search_assignment.csv`). |
| 3 | khornonzon | 16/20 (80 %) релевантных запросов | Flask + ES 8.11, bool_prefix + раскладка/транслит. Ошибки: «ма» (полотенца), «кар тофель» (шоколад), «джин то» (оливковое масло), «хозяйст мыло» (перчатки) (см. `reports/manual_sample_khornonzon_prefix_search_assignment.csv`). |
| 3 | marinasmolkova | 16/20 (80 %) релевантных запросов | FastAPI + OpenSearch 2.11, bool_prefix + rerank. Промахи: «ма» (мармелад/макароны), «кар тофель» (шоколад), «джин то» (оливковое масло), «хозяйст мыло» (перчатки) (см. `reports/manual_sample_marinasmolkova_prefix_search_assignment.csv`). |
| 4 | Cacadude | 16/20 (80 %) релевантных запросов | Flask + ES 8.11, aggressive should-clauses. Попадают «bon pa», «adapter usb c», «масло раст 10л», «power bank 20»; промахи: «ма» (энергетик), «холс», «кар тофель», «хозяйст мыло» (см. `reports/manual_sample_Cacadude_prefix_search_solution_v2.csv`). |
| 5 | iesdai | 16/20 (80 %) релевантных запросов | Flask + ES 8.11, bool_prefix на name/category/brand/keywords. Проблемы: «ма» (энергетик), пусто на «холс», шоколад на «кар тофель», «pr», «хозяйст мыло» (см. `reports/manual_sample_iesdai_prefix_search_solution.csv`). |
| 6 | MathTigran | 15/20 (75 %) релевантных запросов | Flask + in‑memory нормализация/транслитерация. Хорошо покрывает «bon pa», «adapter usb c», «power bank 20», но мимо на «холс», «кар тофель», «pr», «хозяйст мыло» (см. `reports/manual_sample_MathTigran_prefix_search_solution_v2.csv`). |
| 7 | Tasenko03 | 15/20 (75 %) релевантных запросов | FastAPI + OpenSearch + LLM (Gemini via pollinations). Верно: «bon pa», «adapter usb c», «power bank 20», «масло раст 10л». Провалы: «ма» (энергетик), «холс» (протеиновые батончики), «кар тофель», «pr», «хозяйст мыло» (см. `reports/manual_sample_Tasenko03_prefix_search_assignment.csv`). |
| 8 | BRSHD | 14/20 (70 %) релевантных запросов | FastAPI + ES через Docker Hub. Падает на «холс», «кар тофель», «pr», «греч не», «джин то», «хозяйст мыло» (см. `reports/manual_sample_BRSHD_20251108.csv`). |
| 9 | MikR6349880-v2 | 14/20 (70 %) релевантных запросов | OpenSearch 3.0 + ngram + fuzziness; исправлен bulk UTF‑8. Промахи: «ма» (индейка), «холс», «кар тофель», «pr», салаты на «греч не», «хозяйст мыло» (см. `reports/manual_sample_MikR6349880_prefix_search_assignment_v2.csv`). |
| 10 | LazzyBones812 | 14/20 (70 %) релевантных запросов | Flask + ES, batch precompute без `/search`. Хорошо: «bon pa», «adapter usb c», «масло раст 10л», «power bank 20»; ошибки: «холс», «кар тофель», «pr», «масло сл», «хозяйст мыло» (см. `reports/manual_sample_LazzyBones812_elasticsearch_project_for_any.csv`). |
| 11 | VitaliiNovikov75 | 13/20 (65 %) релевантных запросов | Простая bool_prefix стратегия: на «холс», «масло раст 10л», «xfq», «греч не», «хозяйст мыло» и др. уходит в нерелевант (см. `reports/manual_sample_VitaliiNovikov75_prefix_search_solution.csv`). |
| 12 | PopovEgorAndreevich | 13/20 (65 %) релевантных запросов | ES + FastAPI без whitelist; пусто на «ма», «холс», «pr», «хозяйст мыло», шум на «кар тофель» и «греч не» (см. `reports/manual_sample_popov_prefix_search_test.csv`). |
| 13 | andinew79 | 13/20 (65 %) релевантных запросов | Кастомный ES с morphology-плагином + boost-таблица, но «масло раст 10л», «кар тофель», «pr», «греч не» всё ещё промахиваются (см. `reports/manual_sample_andinew79_elastic_search_private.csv`). |
| 14 | 0maffina0 | 13/20 (65 %) релевантных запросов | Flask/FastAPI + ES 7.17: тёплый старт ~5.7 s на первом запросе; промахи на «ма», пусто «холс»/«хозяйст мыло», шоколад на «кар тофель», кабели вместо «adapter usb c», шум на «pr», «греч не» (см. `reports/manual_sample_0maffina0_prefix_search_assignment.csv`). |
| 15 | Aloeseasnn | 12/20 (≈60 %) релевантных запросов | Docker‑стек поднимается, но ручная проверка первых 20 запросов («ма», «санпел», «adapter usb c») выявила много категорийных промахов. |
| 16 | BK-RU | 12/20 (60 %) релевантных запросов | Auto‑метрики ≈0.9, но вручную 8 из 20 запросов ошибочны (санпел, power bank, lay chips и др.). |
| 17 | IRIMURUI | 12/20 (60 %) релевантных запросов | На «холс», «кар тофель», «xfq», «хозяйст мыло» сервис возвращает пусто, а «памперсы 3» и «pr» дополняются нерелевантными товарами. |
| 18 | Wo0druff | 12/20 (60 %) релевантных запросов | Простая bool_prefix схема: «масло раст 10л», «кар тофель», «xfq», «греч не», «джин то», «хозяйст мыло» остаются без релевантных ответов (см. `reports/manual_sample_Wo0druff_prefix_search_assignment_main.csv`). |
| 19 | MikR6349880 | 12/20 (60 %) релевантных запросов | OpenSearch без API: CSV показывает промахи на «ма», «кар тофель», «xfq», «pr», «греч не», «хозяйст мыло» (см. `candidate-evals/MikR-prefix-search-assignment/reports/elasticsearch_evaluation_results.csv`). |
| 20 | DKartsev | 11/20 (55 %) релевантных запросов | Стандартный multi_match + match_phrase_prefix, но короткие префиксы и «масло раст 10л», «кар тофель», «хозяйст мыло» перетягивают мусор (см. `reports/manual_sample_DKartsev_prefix_search_ranking.csv`). |
| 21 | 26Ginger | 9/20 (45 %) релевантных запросов | FastAPI + Elasticsearch 8.11, edge_ngram анализаторы. Провалы: «ма» (энергетик), «bon pa», «кар тофель», «pr», «riesling mos», «греч не»/«prosecco ro»/«хозяйст мыло»; пусто на «xfq», «teos» (см. `reports/manual_sample_26Ginger_prefix_search_assignment.csv`). |
| 22 | Levos06 | ≈9/20 (≈45 %) релевантных запросов | 52/60 запросов прошли авто‑метрику, но ручная оценка тех же 20 запросов показала <50 % релевантности. |
| 23 | Vladospb | 8/20 (40 %) релевантных запросов | Матча вместо «ма», индейка вместо «bon pa», лампы на «холс» и творог на «power bank 20» — ранжирование не привязано к запросу. |
| 24 | Torrin0 | 7/20 (35 %) релевантных запросов | Каталог всего на 23 SKU: многие запросы пустые, а «adapter usb c» и «джин то» возвращают сыр и масло. |
| 25 | Zanegin-n | 6/20 (30 %) релевантных запросов | Whoosh + Flask: спасает «йогурт гр», «крем для рук», «xfq», но «bon pa», «maslo раст 10л», «power bank 20» и др. дают лампы/кофе или 500‑ошибки (см. `reports/manual_sample_zanegin_CargoPrefix.csv`). |
| 26 | marinasmolkova | 0/20 (0 %) релевантных запросов | Все 20 запросов вернули 500 на `/search` (см. `reports/manual_sample_marinasmolkova_prefix_search_assignment.csv`). |

Все оценки — ручная проверка первых 20 открытых запросов из `data/prefix_queries.csv` (от «ма» до «power bank 20»). Если сервис не вернул релевантный товар или дал явный мусор, запрос считался проваленным.
